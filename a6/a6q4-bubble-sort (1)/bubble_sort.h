// bubble_sort.h [INTERFACE]
// A module that performs bubble short on an integer array

// bubble_sort(a, len) performs bubble sort on arr
// requires: len must be positive
// effects:  might modify a
// Efficiency: O(n^2)
int bubble_sort(int a[], int len);