// largest-sum.h [INTERFACE]
// A module that calculates the sum of the n largest elements in an array

// largest_sum(array, len, n) calculates the sum of the n largest 
//   elements in the array
// requires: len > 0, n > 0 && n <= len
//           array must not be mutated
// effects: none
// time: To be determined by you. Add info the the implemenation.
// NOTE: Try to implement this as efficiently as possible
int largest_sum(const int array[], int len, int n);
